# Validation

## Static gates

- `make autoware-build`: passed, 25 packages.
- `test_race_mpcc_foundation --gtest_color=no`: passed, 19/19.
- `git diff --check`: passed.

The focused tests cover moving, stopped and opening-gap horizons, stale and
missing identity, current hard-gap violation, malformed target kinematics,
malformed configuration, Follow eligibility and warm-start identity.

## Dynamic root-cause evidence

### Before coherent-front admission

Run: `output/20260823-124531`

The first eligible Follow cycle reported:

```text
final=Follow target=d2 front=0 fd=inf fs=inf locked_seen=0
Follow MPCC shadow: contract=0/invalid-configuration
```

The same window accumulated 41 eligible cycles and zero contracts or solves.
The observation proves that the failure was not a gap parameter: a retained
Follow action without a current front observation crossed the shadow boundary.

### After coherent-front admission

Run: `output/20260823-125643`

- Retained Follow labels without a finite front observation no longer started
  Follow shadow construction or solve.
- The physically observed front vehicle in this run was stopped (`fs=0`) and
  correctly classified as `LowSpeedAvoidance`, not moving Follow.
- No Follow shadow result was selected; production authority was unchanged.
- Moving-front Follow was not generated by this scenario, so positive dynamic
  coverage remains outstanding and production promotion is prohibited.
- Callback overruns occurred in both domains while Follow shadow was inactive;
  they are existing production/low-speed-path evidence, not attributable to
  this shadow Slice.

Short diagnostic confirmation: `output/20260823-130902`

Both domains emitted the typed boundary exactly once on the status transition:

```text
eligibility=no-coherent-front-observation
status=not-eligible
authority=shadow
selected=0
```

The adjacent behavior line showed `final=Follow`, `front=0`, `side=1`,
`fd=inf`, and `fs=inf`, confirming that the rejection names the earliest
invalid semantic boundary rather than a downstream solver/configuration error.

## Authority audit

- No publisher connection was added.
- No production selector, speed output or steering output was changed.
- No config, feature flag, timeout, lease or fallback was added.
- The legacy scalar Follow cap remains the production owner.
- Invalid target observations fail before QP assembly and cannot be repaired by
  a clamp.

## Next gate

Obtain one deterministic moving-front scenario with finite `fd/fs` and confirm:

1. contract construction and dedicated Follow solve occur;
2. target ID/generation remain stable across the horizon;
3. the result is finite and physically certified;
4. the result remains `authority=shadow, selected=0`;
5. no callback overrun is attributable to the additional solve.

Until that evidence exists, do not promote Follow or delete its legacy owner.

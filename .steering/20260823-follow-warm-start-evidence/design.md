# Dynamic evidence design

Run `make dev2` with production parameters and authority unchanged. Redirect only the simulator working
directory to an ignored output folder. Aggregate the one-second Follow runtime windows rather than using
status-transition logs as cycle counts.

The comparison boundary is deliberately narrow: warm-start application and the first failed acceptance
stage. Race penalties are recorded for context but cannot be attributed to a shadow-only command.

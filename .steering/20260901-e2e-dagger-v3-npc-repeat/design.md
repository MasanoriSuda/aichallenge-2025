# Design

Repeat `e2e-npc-single` with start seed `2027`.  No model or controller
configuration changes are permitted.  Compare race, motion and authority
statistics with seed `2026` (`output/20260901-190146`).

Two successful seeds qualify v3 as the leading experimental dynamic-obstacle
candidate, but do not automatically replace the conservative production
default.  Promotion still requires an explicit documented decision.

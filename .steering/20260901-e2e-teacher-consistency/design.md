# Design

The audit runs the current `LidarPrecontactTeacher` and frozen candidate3 over
both immutable corpora.  Teacher sequences are checked against their stored
base and successor steering.  Normal sequences are compared with their
declared zero-residual semantics.  Material fractions and teacher decision
reasons distinguish source/version drift from a policy-label contradiction.

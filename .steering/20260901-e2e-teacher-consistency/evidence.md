# Evidence

Report:

`output/20260901-e2e-teacher-label-consistency.json`

## Stored teacher reproducibility

The current frozen candidate3 plus `LidarPrecontactTeacher` reproduces every
stored recurrent teacher base and successor steering value exactly:

- train maximum base error: 0.0 rad
- train maximum teacher error: 0.0 rad
- validation maximum base error: 0.0 rad
- validation maximum teacher error: 0.0 rad

There is no teacher-version or recurrent derivation drift.

## Zero-normal contradiction

Applying that same deterministic teacher to the declared zero-normal corpus:

- train: 1,907 / 13,588 material corrections (14.03%), mean abs 0.02179 rad
- validation: 662 / 6,126 material corrections (10.81%), mean abs 0.01970 rad

The zero target is false under the teacher policy.  More importantly, every
source in `dagger_aggregate_v2` is labeled `lidar_gap_teacher` or
`lidar_gap_teacher_dagger`; none is a frozen production candidate3 run.  One
train sequence generated by the historical gap teacher has a 52.21% material
correction rate under the successor teacher.

The previous normal anchor and independent-normal Gate therefore used the
wrong world-state distribution.  The correct replacement source is an admitted
`fixed_lidar_brake` candidate3 run with matching checkpoint SHA, completed laps,
zero penalties and no stall.  `output/20260901-151131` is one such run and has
3/3 laps, zero penalties and exact candidate3 SHA.  Additional run-disjoint
production evidence is required before rebuilding train/validation anchors.

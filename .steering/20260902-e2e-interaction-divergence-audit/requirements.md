# Requirements

- Freeze the packaged v11 lateral authority and both production longitudinal
  defaults while diagnosing the four-vehicle interaction failure.
- Compare failed/recovered domains from the same immutable final-world run.
- Separate pre-contact evidence from post-contact symptoms.
- Determine whether the current ML command is weak or opposite to the
  pre-contact teacher, whether the requested escape side flips, and whether
  the same condition occurs in clean domains.
- Do not train, promote, or tune a model in this Slice.
- Preserve the E2E runtime input contract and all ROS/evaluation interfaces.

# Pass progress audit

User-authorized MPCC completion. Run4 now crosses stateless ShiftOut to
certified Pass, but no complete overtake is observed. D1 remains behind D2
and enters Recovery through progress watchdog at decision3465.

Classify measured motion, filtered target motion, commanded/optimized speed
and objective/horizon semantics before proposing any production change.
Do not change speed, margins, gains, watchdog or retry limits to hide the
boundary. Recovery follows the failed tactical objective and is not itself
the root cause. Source3465 is captured after the phase-transition boundary;
it must not be described as the exact preceding accepted artifact.

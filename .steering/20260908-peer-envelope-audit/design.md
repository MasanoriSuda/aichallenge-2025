# Compared physical representations

A: current circle from lateral widths. Cheap but under-encloses527of705 solid
body vertices in XY about the antenna. Reject as complete-body geometry.
B: circumscribed3D sphere projected to a2D circle centered on V2X point.
Nominal radius1.876m contains the inspected body for every rotation. More
conservative laterally; test passage feasibility rather than reducing it.
C: oriented convex hull/rectangle. Tighter but direct yaw is absent from V2X.
Motion heading is not a verified orientation bound for stopped/slipping cars;
requires an explicit observation/uncertainty contract before adoption.
D: union of heading-bounded shapes. Can reduce conservatism only with a
validated heading bound; with unconstrained orientation reduces to B.

Freeze a synthetic overlap witness and call the actual native circle/footprint
predicate. An old positive clearance during measured body overlap is a model
counterexample, not evidence that a full published racing command bypassed
all independent tactical guards. The latter may mask this geometry defect.

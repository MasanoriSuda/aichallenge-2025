# Current-world complete-rest candidates

Continue full autonomous MPCC completion from3c1cd03b. No simulator/build running.
Current consumer, exact Stop pose and clock ownership are locally validated;
last dev2failed D1world4264with actual3695. Rollback3c1cd03b.

Before production: replay actual3695without solving against world4264and
compare bounded candidate architectures. Preserve all prior worlds/outcomes.
World1629free-rest candidate already passes exact current-world command proof
and conditional188intervals; its producer remains offline. Maximum-braking-only
normal candidates are an incomplete family, not a physical infeasibility proof.

Design hypothesis: full-horizon canonical controls with explicit terminal rest,
original physical bounds and racing objective, and all immutable peers in
Cartesian supporting constraints. Separate terminal-policy identity from the
upstream normal problem. Compose full peer rows into one broad QP around the
same wall-only witness before one solve; prove all walls/peers/actuation/rest
again on the exact model. No peer omission, assumed cooperation, margin/weight
or solver-iteration tuning, additional retry/hold/grace/fallback.

Before promotion: bounded same-scene witness, native failing production-path
regression, immutable identity and real async-join tests. Replace the old
maximum-braking-only current-world normal candidate path; preserve explicitly
historical offline comparisons and independent Emergency. Keep seven-state
normal authority and all source/artifact/proof/command identities. Build and
package tests, one fixed single/dev2campaign, then remaining intents/dev3/dev4,
gates and baked submission. Do not edit production during build/runtime or
run heavy replay alongside simulation. Full acceptance remains open.

## New4264earliest invariant

Zero-solve actual3695reproduces terminal-wall rejection and zero Stop pose error.
Current source A/B/C/D/G and free-rest all fail initial adapter at stage19:
theta18.78508273576574+nu11.253128136429144*dt0.08998357833163975
extends past immutable frame19.784900272783947. The previous tangent-domain
repair applied only after a QP iterate existed; initial soft references have
the same coupled nonlinear domain. Native13mm reference overshoot fails before
production. Share valid tangent selection for initial and subsequent stages,
keeping objective/reference/constraints/raw results unchanged.

Separately, maximum-brake candidate rejects because original horizon ends
before physical rest. This falsifies a fixed-source-time free-rest solution
for high-speed4264. Any retimed candidate must reseal horizon/terminal identity
and recompute all peer clocks, within original dt limits; no silent retiming
or physical infeasibility claim. Compare offline before candidate promotion.

## Promotion boundary now under validation

The bounded full-frame constant-deceleration clock4.543438906s and source-max
clock5s both reject4264. Maximum-law candidates reach a solved trajectory but
exact wall proof rejects; free-rest candidates reject wall QP. All remain
Unknown physical feasibility. By contrast1629's5s free-rest arms0/3 pass exact
wall/all-peer/rest and current command join; maximum-law arms0/3 reject rear
QP. The current observation's13mm initial tangent regression fails before and
20adapter tests pass on the source-copy fix. The production-path native scene
with rear+side peers fails before at dynamic clearance-0.0110606m.

The producer replaces only evaluate_current_world's maximum-law family by
free canonical controls with terminalv=0 and the original maximum stage dt.
Source geometry, model, observation/control origin, physical boxes and all
weights remain. Changed stage clocks recompute peer predictions. The source
bounds-schema receives /free-controls-rest-uniform-max-dt-v1 and is resealed;
solver validates its full-horizon/rest/uniform-clock meaning. Upstream normal
identity remains the async mailbox/scope owner. The maximum-law and lattice
APIs remain labelled historical publisher-boundary/offline comparisons; the
current-world path returns after its single direct candidate, including failure.

All observed peer rows are generated from one wall-only witness and composed
before a single dynamic solve. refine() clears previous peer rows, so passing
its result into the next refine would silently drop a peer; explicit row-set
concatenation is used. Primary explicit tactical topology is retained; secondary
peers use full oriented physical support. The offline multi-SQP branch rebuilds
all these same peers, not only primary. Full exact all-peer and wall proof still
owns acceptance. Initial and subsequent tangents now share one coupled-domain
selection helper, leaving soft objective/reference/boxes and raw iterates intact.

This does not claim a4264rescue or integrated acceptance. Fresh single/dev2must
establish whether earlier valid complete-rest candidates preserve real authority.
No new retry/hold/grace/fallback, margin or solver-budget change is introduced.

The first full package test catches an old short-wall-profile source with
progress upper5m but supplied wall profile ending2.4m. Maximum-law controls
never explored that mismatch; free controls expose no-covering-segment. The
new candidate intersects future progress hard boxes with BOTH immutable wall
profile and course-frame domains before solve. It does not widen the wall or
clamp raw solutions. Source initial pose and soft objective remain unchanged.
The original acceptance test is preserved and now passes in the focused native
run alongside the rear+side regression. Explicit wrong stage clock, missing
terminal rest, all-peer row count and stale secondary provenance checks are
included. Final build/package/run evidence is still pending.

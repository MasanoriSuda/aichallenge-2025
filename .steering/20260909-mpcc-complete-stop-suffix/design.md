# Complete stopping suffix ownership

Continue autonomous MPCC completion from8d6ebf8a. No simulator/build running.
Baseline/rollback8d6ebf8a. No new normal authority before failing native evidence.

Two sealed inputs motivate this slice: world1629has3offline feasible canonical
full-control trajectories through rest, but they have no publication authority.
World968contains actual354; published Stop successor certifies and is reified
as bundle439, then the retained consumer rejects a newly generated terminal.
First reproduce both with complete source/artifact/proof ownership. Never use
new offline artifacts as replacements for actually published354or1100.

Hypothesis: a complete physically certified through-rest control suffix is
replaced by a different maximum-braking/path-reference trajectory in retained
validation. A complete suffix re-proved from the current state could discharge
the terminal obligation itself. Require complete horizon, exact rest, same
publisher interval, actuation bounds, wall, all peers, Follow/Return semantics,
current observation and immutable artifact/clock identity. Partial-only proof,
old state proof, or planned-rest label alone must never grant authority.

Audit multicycle and async joins before promoting candidate generation. If this
hypothesis is falsified, record the producer actually failing; do not install
retry/hold/grace, tune margins or discard peers. Free-control candidate generation
must retire the replaced maximum-brake-only normal candidate path in its slice;
independent Emergency remains separate. Do not edit production during builds or
runtime. Native failing/passing, package, build, same-world replay then fresh
single/dev2; independent intents/dev3/dev4/gates/submission still required.

## Earliest proved producer defect

Native968replay reproduces original failure and certified-successor/reproof
failure. Before any suffix reuse, the bundle's initial Frenet pose reconstructs
37.925040988mm away from the same current control pose, yaw error-0.002027228rad.
The producer's two-step loop updates course progress after projecting Frenet
coordinates and never reprojects at that final frame. Bundle then reconstructs
progress as lifted progress minus the stale lag. A simple straight-frame
association with nonzero lag demonstrates the same translation.

Repair: own the exact selected course coordinate separately from associated
physical progress; project once at that coordinate and carry it unchanged into
physical Stop integration and bundle construction. Remove the stale fixed-point
iteration and derived bundle coordinate. No physical pose, wall tolerance,
terminal requirement or authority is changed. This may correctly reject the
previous falsely certified successor; that is not an availability fix.
Full-through-rest architecture remains next, not implicitly promoted.

The first native helper build used the system GTest headers against ROS's
vendored GTest library and failed to link. Its output is preserved; using the
actual CMake include path then reproduces the150mm wrong initial position.
The968replay helper's first build used a nonexistent enum and failed; corrected
TimeAlignedCandidate matches the production join. Neither build failure is
counted as a controller regression or test pass.

Promotion must explicitly own terminal semantics in immutable candidate identity:
offline comparison fingerprints include terminal bounds, but production context
must not allow free-rest and racing-prefix requests to alias. Source/control
trace capture has no Store or publisher authority. Do not infer that an offline
ManeuverBundle can execute because it holds static/dynamic certificates.

## Complete suffix terminal consumer

A new native straight-road witness accelerates within original bounds before
braking to rest while a rear peer approaches. Its entire wall/dynamic suffix
is clear, but the old consumer replaces it by immediate maximum braking and
rejects terminal proof. The original1629fresh free-control artifact likewise
passes all current-world full-suffix proofs and is rejected only by the newly
generated maximum-braking sequence (dynamic minimum-0.006402223m).

Carry exact dense applied actuator samples from physical continuation. Only a
complete current-state continuation with full static/dynamic/Follow proof,
full remaining controls, publisher interval coverage and actual terminal rest
can itself discharge the Stop obligation. Recheck an active semantic terminal
contract against its actual current-world endpoint. Original planned rest or
publisher-only clearance never qualifies. Replaced path: unconditional
maximum-braking regeneration for already proved complete stopping suffixes.
The previous terminal generator still handles moving/partial normal horizons;
there is one existing production authority and no new candidate/hold/fallback.
Telemetry identifies solved-stop-suffix. Free-control candidate production is
still unpromoted; its immutable terminal identity and multicycle joins remain next.

The first conditional repeated-publication helper stopped with a publisher
interval mismatch before producing its summary. Its binary/input/artifact/log
are preserved. A native fixture with physical tolerance0.016 reproduces the
new suffix metadata selecting35ms instead of the exact25ms boundary. The
existing footprint prefix index includes physical residual allowance; it must
not be reused as a command clock. The new suffix consumer now selects its
publisher sample count by exact event time using the existing identity-time
tolerance. No existing wall/peer margin or solver tolerance is changed.

The package's source-shape test initially failed because ActuationSample was
moved to a shared PhysicalActuationSample with a compatibility alias. Update
that structural assertion to require both shared type and alias; retain all
publication-before-observation checks. Full60groups/2366records then passed
before the newly discovered publisher-boundary correction; those logs are
preserved separately from final verification.

With the exact publisher boundary, conditional same-artifact replay accepts
all188successive25ms joins through rest without another solve. It keeps the
original130ms observation/control delay, uses the original predicted prefix
then exact applied-interval model states, CV peers, linearly interpolated early
current speed and a fixed current-time steering diagnostic. No future measured
truth or live publication is claimed. This permits integrated validation of the
consumer, not automatic promotion of the offline candidate producer.

## Candidate-generation promotion design to validate next

The production current-world Stop worker still prescribes maximum braking and
zero objectives. The new offline witness retains original physical limits and
racing objective, sets only full-horizon terminal rest, and exposes the rear
peer to physical supporting planes. A terminal-policy identity must separate
that candidate from the upstream normal problem before any production solve.

Prefer composing full physical separating rows for every immutable ReplayWorld
peer into one broad QP around the same wall-only witness. Existing single-peer
refine already returns an AssemblyRequest; independent rows may be composable
before one solve. Each peer must use the same Cartesian stage clock, full body
and observation generation, preserving explicit tactical constraints where
applicable. The exact all-peer nonlinear proof remains mandatory. Verify this
composition on sealed scenes before changing the live producer; no special
rear-peer omission, repeated retry, topology timeout or cooperative prediction.

Promotion replaces the maximum-braking-only current-world normal candidate
path, while historical offline maximum-braking comparisons and independent
Emergency remain separately labelled. Preserve result.source_normal_identity
for async submission ownership and reseal candidate terminal semantics;
validate candidate/source/observation/clock ownership at actual adoption.

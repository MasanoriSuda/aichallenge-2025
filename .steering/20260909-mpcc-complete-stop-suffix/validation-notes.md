# Validation scope

Before-production failures are independently preserved: world968initial
pose error37.925mm; native associated-progress pose error150mm; native safe
free-control terminal rejected by synthetic maximum braking. Initial helper
build/link errors are separate from these controller failures.

Dense applied controls now belong to the already built continuation. Only
complete current-state, all-wall/all-peer, semantic-terminal and exact-rest
proof can discharge Stop without a different trajectory. The25ms metadata
regression prevents physical residual units from changing command duration.
Speed-perturbed cases reject planned-rest reuse. The existing104source-contract
checks retain serialized-publication-before-evidence promotion.

Final25package build and60CTestgroups/2366records pass, all67retained native
cases pass. Intermediate2365pass, renamed-type assertion failure, corrected
2366passes and boundary correction outputs are separately preserved. Colcon's
archived stdout restores the first pose-only package log; later runs reused the
temporary package-log path, not its preserved result outcome.

The new free-control candidate artifact/source are captured offline. Conditional
replay contains one solve and188joins;170use solved-stop-suffix,18use the existing
independently certified generated terminal. CV peers and model-derived future
ego observations are assumptions, not measured runtime evidence. Current-time
steering is fixed diagnostic and early current speed is interpolated. One aborted
helper exposed the35ms versus25ms metadata issue; no runtime outcome is inferred.

Each live run pins the final source diff, inputs, binaries and image identities.
No source changes, builds or heavy replay run alongside simulation. Read bag
only after teardown. Single succeeds; dev2outcome is added to results.md on
closure. Free-control candidate generation remains offline; async promotion
and the full MPCC acceptance remain open. No pre-commit hook execution claimed.

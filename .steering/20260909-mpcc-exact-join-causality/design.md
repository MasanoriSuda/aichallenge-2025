# Exact current join and candidate causality

Continue autonomous completion after466bba5e. No production change is selected.
Earliest active moving failure is new dev2 D1world945/actual361, with exact
rejected Request and PublishedPlan clock. Revalidation now reproduces without
solving: steering-unreachable and no viable terminal peer proof. This is a
correctly observed rejection, not yet its upstream cause.

Before any failure-family repair, align the same-run serialized predecessor,
source/receipt clocks and exact Request. A clock/command ownership defect must
fail an exact invariant, not merely show physical tracking error. Separately
compare A/B/C/D and four explicit complete-rest formulations on sealed945,
retaining wall/peer geometry, original constraints and changed candidate identity.
Earlier923and1629need different accepted offline Stop candidates;4264Unknown.
No repeated old comparison is needed unless a recorded revisit condition is met.

No weight/margin/rate/timeout/iteration tuning, alternate normal authority or
offline-to-production promotion is authorised by an accepted diagnostic alone.
If a producer defect is demonstrated, create a failing native case, remove its
obsolete path, build/regress/replay, then perform fresh single/coupled acceptance.
Rollback is466bba5e. Do not build or run heavy replay during a simulator run.
All original artifacts and user files remain protected. Full acceptance includes
moving Stop/restart, all intents, dev3/dev4, gates and same-artifact evaluation.

## New evidenced upstream producer defect

945predecessor matches the serialized float32 command with sealed gain1.435;
the20ms control-clock age also matches. Nine normal A/B/C/D/G arms, four
support Stop arms and four full-rest missions reject. An independent45arm
physical stopping-controls probe also rejects; this is not an infeasibility
certificate and does not verify every original seven-state QP state constraint.

Same-source-time V2Xd2GNSS positions and d2GNSS poser output differ by+0.26m
along mapX, near-zero alongY. Output quaternion remains identity while d2
heading is about2.45rad. GNSSPoser(use_gnss_ins_orientation=false) compares each
sample displacement with0.2m, then unconditionally resets its static previous
position even when no heading is accepted. At20Hz/1.8m/s, every displacement is
below0.2m, so cumulative motion never supplies an orientation. The uninitialised
quaternion becomes the axis-aligned antenna-to-base transformation; the downstream
imu_gnss_poser sees a finite identity and cannot correct the displaced position.

Invariant: subthreshold position samples accumulating beyond the declared
spatial heading baseline must update direction, independently for each node.
The accepted direction must rotate the existing antenna/base lever arm. Do not
change0.2m, sensor covariance, V2X peer geometry or MPCC proof limits. Establish
an actual-node failing sequence before repair. Remove function-static/shared
position and unconditional anchor reset; preserve the accepted anchor until a
new heading observation is usable. Initial no-motion heading remains separately
unobserved; do not assert an accurate stationary yaw or complete live attribution.
Acceptance includes slow north/west/reverse sequences, node independence, rotated
lever arm, covariance regression,25package build, affected package tests, same-bag
replay and fresh single/coupled runs. Rollback remains466bba5e.

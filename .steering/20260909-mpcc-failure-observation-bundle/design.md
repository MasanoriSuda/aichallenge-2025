# Atomic authority-loss observation bundle

Continue autonomously from6c4875ed. The overall MPCC acceptance remains open.
New run20260909-committed-longitudinal-dev2-r1loses D1normal authority at4428,
8.42m/s, terminal wall contingency. Current world4428is saved; the actual
published3834solver source/artifact is missing. Earlier startup296already
consumed the independent published-source recorder's first-per-failure key.
The terminal failure observer also omits the publication snapshot argument.
Never substitute startup296or a re-solved3834candidate for the actual artifact.

Earliest observation invariant: every written authority-loss world must carry
the corresponding immutable publication source, artifact, clock and failure
fingerprint in the same atomic record. Separate per-reason deduplication can
write the world and silently discard its executed source. Existing first-key
bounding is retained; there is no higher snapshot quota or unbounded logging.

Before a further control change, compare A/B/C/Dand fresh Stop on sealed4428
with production parameters unchanged. The actual executed3834cannot be replayed
yet. Capture that limitation alongside every accepted/rejected/inconclusive arm;
all-arm rejection without a certificate stays Unknown physical feasibility.

Observation repair scope: a proof-failure bundle embeds the current world plus
the published solver input/execution/clock. Validate every identity before
serializing; asynchronous writer owns all bytes and publishes one directory
atomically. Preserve the current world even if publication evidence is absent
or invalid, with an explicit absence/rejection reason. Wire command, solver,
state prediction, runtime margins/parameters and normal authority stay unchanged.
Retire the production two-independent-record path in this same slice. The
standalone old-source recorder can remain as an offline compatibility API.

Native failing reproduction must show two distinct recorded failure reasons
with different published artifacts in the same intent; the second source must
not be lost to the first generic publication key. Also verify invalid source/
artifact/world fingerprint, missing publication, duplicate behavior, round-trip
payload and atomic directory output. No observation result is a controller fix.

After focused/package tests and25-package build, run a bounded unchanged-control
dev2diagnostic with inputs/main/DSO sealed. Stop at first active moving failure
or full completion; preserve bootstrap failures and every result. Use its exact
tuple to reproduce the first physical rejection and identify its producer before
any control edit. Rollback baseline6c4875ed. No simulation overlaps production
edits/build/heavy replay; user JSON preserved byte-for-byte.

## Confirmed comparison and implementation boundary

World4428fingerprint9722352782942114879: A/Bleft/right/Gleft solve but fail
terminal wall proof (first sampled Stop contact at stage107,6.448m), C/Dleft/right
and Gright reject in the dynamic-plane QP.9normal arms bundle0. Fresh Stop input
fails before its arm expansion: maximum-braking horizon ends before rest. This
is not a four-arm physical infeasibility result. No parameter changed.

Native before test writes two different failure worlds but the second actual
source is Duplicate under cruise|side=1|physical-proof|published-source-at-authority-loss.
The production repair embeds source/execution/independent wall payload/clock in
one current-world record. New publication_bundle/v1status distinguishes present,
missing and invalid while old top-level world parsing remains compatible.
The two producer observation calls also competed through submit_latest; when
this decision's terminal observation is enqueued, skip its redundant generic
normal observation so it cannot replace the more specific record. This local
submission result has no command/solver/Store effect. Both record producers read
the same existing latest-publication ledger before it is cleared.

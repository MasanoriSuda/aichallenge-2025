# Validation ledger

- Baseline6c4875ed4d250a44cd813935b234863e9c184701; observation changes only.
- New-world architecture comparison: all9normal arms bundle0; A/Band Gleft
  reach terminal wall rejection, C/Dand Gright fail dynamic-plane QP. Fresh
  Stop comparison cannot expand arms because the current horizon ends before
  rest. These are bounded method outcomes, not physical infeasibility proof.
- Before-repair native regression fails the second-source invariant: both
  current worlds Written, second published artifact Duplicate. Log and source/
  built-library hash in output/20260909-failure-bundle-before/.
- Initial new tests rejected an invalid Cruise fixture carrying an Overtake
  execution side. The fixture now uses the existing valid neutral Cruise
  publication setup; no production guard was relaxed. Updated native suite
  14/14passes, including distinct worlds/artifacts, separate grid payload,
  fingerprint/clock/source/semantic mismatch, missing evidence, deduplication,
  atomic directory and unchanged-loader round-trip.
- Source-contract suite104/104passes with PYTEST_DISABLE_PLUGIN_AUTOLOAD=1.
  The first host command failed collection because the auto-loaded ROS launch
  plugin imported unrelated localization_scope; that error is preserved.
- Full25-package build passes3m55s (setup.py deprecation only); all60CTestgroups
  pass and scoped colcon result2362records/0errors/0failures/0skips.
- Bounded observation-only dev2 diagnostic is stopped. D2 decision1629 at
  4.3429252929m/s loses terminal dynamic proof against D1; current world and actual
  source1100/execution/clock are present in one atomic record. Observation
  acceptance passes; integrated driving acceptance fails. Historical3834remains absent.
- Zero-solve native replay certifies the original1100 wall/dynamic horizon, then
  reproduces1629's two rejected terminal references. Track reference clearance
  -0.0067710926225m at1.555s,573checks; normal reference -0.0061931006377m at1.56s.
  Continuation and delay prefix pass. Terminal wall checking was not reached;
  its default invalid-rollout field must not be called a wall collision.
- Physical current steering is rounded from log to six decimals; current speed
  comes from exactly matching odometry pose/stamp. Local non-wrap replay uses
  an explicitly declared1000m unused course-length surrogate. Replay matches
  recorded terminal clearance to printed precision; no solve reconstructs execution.
- Same1629world: A rejects stage19linearization; B/C/D/G cannot construct a
  front-target tube because this Cruise scene's only peer is behind. Four fresh
  Stop methods solve but all reject dynamic proof against D1 around1.50409s.
  These are method limitations/rejections, not physical infeasibility evidence.
- Held-out D1GNSS at the first rejected instant clears by0.063/0.069m, but the
  full hypothetical stopping path still overlaps the full peer circle by0.096/
  0.103m. D1's later Emergency also changes its motion. Therefore the hypothesis
  that merely replacing CV with the observed future explains away this failure
  is rejected. No narrower peer geometry or future braking is authorized by it.
- Ready/Start before shutdown: each Domain reports1178callback cycles;
  D1max16.038ms, D2max21.060ms, neither overrun. Command receipts D1max34.994841ms,
  D2max40.645838ms, neither gap>50ms. D1has one65ms source-stamp gap; sensor/clock
  receipt stalls remain separate. No finished result-details or penalty acceptance.
- Both user-owned JSON inputs are restored with their exact prior SHA-256.
  Source, runtime libraries, snapshots, generated native copy, every comparison
  result and diagnostic timing are sealed in evidence.json. Overall acceptance
  remains P2partial/P3/P4open. See results.md for the next bounded comparison.

# Shared longitudinal command/physical model

Baseline ae2efe6a; controller unchanged from1c4f377e. Continue autonomous completion.
Prior slice directly measures latest-value application and reproduces wire/net
mismatch. Full acceptance remains open; no production model selected yet.

Earliest candidate invariant: the same serialized acceleration input must mean
one physical longitudinal response throughout now-prefix, canonical seven-state
SQP, nonlinear physical proof, retained/complete Stop, immutable artifact and
publication. The paired response correction currently stops at the0.13s prefix.
Native current model differs74mm/s in each isolated rolling-only fixture.

Compare offline before production changes: current wire=net model, causal held
response term across horizon, and explicit local resistance/drag components.
Keep wire bounds, steering, solver, wall/peer limits unchanged. Bind every changed
model parameter/schema to a new context. No diagnostic application trace in
production decisions. No future measurements in the estimate. Unchanged all9
normal+4supportStop on instrumented933 remain rejected/Unknown.

A constant negative offset is not automatically an admissible physical model at
rest: actual rolling resistance opposes motion and vanishes/caps atzero; positive
compensation intended to balance a moving-state loss must not drive a stopped
vehicle. Check rest/stopping semantics before selecting a held-offset design.
Also separate response estimate from uncertain command selection/latency; the
old0.13s shared origin is not an exact longitudinal application phase.

First bounded work: inspect actual rolling/wheel/brake/rest producer, create
observation-only model comparisons using native seven-state transition and
same-run pre-Emergency input/velocity windows. Establish acceptance/rollback
before unified source/schema/serializer changes. No gain/delay/rate/hold/margin
or solver tuning. Planned completion: native/replay, build/package, single/dev2,
all intents/async/dev3/dev4/gates, same-artifact submission/eval and local commits.

Comparison outcome: the flat held-response proposal is rejected at rest and on
independent dev2 data. A measured-state-conditioned wheel model improves moving
interval error, but uses future measured lateral/yaw state and cannot be promoted.
Runtime physics confirms contact-dependent forces and YAML grip clamping .6 to.7.
No production model is selected. See [results](results.md) for exact limitations
and [completion plan](completion-plan.md) for the bounded implementation sequence.
Rollback baseline is ae2efe6a (controller1c4f377e); observation helpers are never
part of the participant/submission and are mounted only in diagnostic runs.

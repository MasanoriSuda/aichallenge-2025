# Observation repair review

Earliest broken invariant: a valid CertifiedPlan can lack a solver snapshot;
the observer discards its actual artifact/physical evidence at the solver-source
gate. Actual native fixture accepts its physical proof and fails the old recorder.
Producer repair: retain the immutable plan at the already-owned publication
observation boundary; independently serialize artifact/context/frame, physical
snapshot/proof/grid. No solver/proof acceptance relaxation or command authority.
Existing solver-source fields retain missing status. Invalid actual-publication
association stays invalid without contaminating current-world observation.

No normal authority is added/replaced. Source-context serializer is factored
without changing its fields or fingerprints. Source-free evidence is additive;
old schema loaders and result JSON are unchanged. Memory is bounded by the
existing first-boundary FIFO and already shared immutable plan owners; grid
serialization, validation and I/O run on the observation worker. Separate grids
are written inside the existing atomic directory transaction.

Native regression18/18 and source contracts104/104 pass. Original valid physical
proof re-evaluation after full recorded decode matches every diagnostic field,
including NaNs; no solver invocation. Local replay decoder intentionally refuses
to drop a present solver source; source-present evidence uses the existing replay.
Compiler/program launch failures are preserved with their separate causes.
Runtime dev2 records the complete actual387and exact930/931Requests. D1/D2
callback maxima20.668/26.215ms; the one D2overrun reported window follows first
D1Emergency. Command receipts~40Hz/no >50msgap; source and observation delivery
pauses remain. Observation acceptance passes; integrated race does not.

No live observation from old run930/385 can be recovered retroactively. A new
bounded diagnostic must retain its own run/world/artifact identity. Parent
derivation/dynamic proof is not available in CertifiedPlan and is not invented.
Rollback is the observation-only production diff against36c2a01a; retained data
and rejected outcomes remain evidence. Full MPCC acceptance stays open.

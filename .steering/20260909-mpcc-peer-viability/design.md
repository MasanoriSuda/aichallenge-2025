# Current-world peer and stopping viability

Continue authorised completion after9d76066c. Production is fixed for diagnosis.
GNSS cumulative heading and first-final observation repairs are locally accepted;
fresh single6laps passes, coupled fails. New firstmovingD1world941fp5417572569565392666
and actual/inspected370with exact Request are preserved. Zero-solve steering join
passes; terminald2clearance-0.001206851953m and independentStop-0.000951979315m.
No callback overrun. Source/receipt delivery still has separate gaps.

Compare A/B/C/D and explicit full-rest formulations on941before another patch
in this failure family. Old945all-method and45physical-profile rejection is not
an impossibility proof, and its GNSS world is not substituted for this one.
Inspect the peer position/velocity/uncertainty producer and the preceding viable
terminal observation. Only an evidenced invariant failure may select a repair.
Do not change covariance semantics, peer margins, timeout, rate, weights or
iteration counts merely to alter acceptance. An actual unit/provenance defect
requires a failing native test, correct source ownership and retained strict proof.
No new normal authority or unproved offline candidate promotion.

Rollback9d76066c. No simulator during heavy diagnostics/build; no production edits
during build or simulator. Preserve user artifacts and all rejected trials.
Full moving Stop/restart/intents/dev3/dev4/gates/submission acceptance stays open.

## Fixed-world results and falsified hypothesis

941 normal A/B/C/D/G: all nine solver-rejected. Support Stop: two solver-rejected,
two exact dynamic-proof-rejected (minimum -0.00034656m). Explicit full-rest
free/inherited and free/zero both solver-rejected; maximum-law/inherited and
maximum-law/zero are wall-clear but peer-rejected (-0.000325794/-0.000325568m).
These first-overlap clearances are detection samples, not deepest penetration.
All-method rejection remains Unknown. No physical infeasibility certificate.

The misleading ReplayWorld covariance_*_m2 names do not establish a unit bug:
V2XVehiclePosition.msg defines standard deviations in metres. Runtime radius
1.876 +0.05 +0.005=1.931m uses those wire values consistently. Do not apply sqrt.

## Earliest demonstrated observation defect and bounded repair

941 exact rejected Request exists, but accepted_boundary=unavailable. The
controller retains accepted boundary metadata only for ShiftOut/Pass, and
evaluate_rate_resolved_track_cruise_plan discards Requests when proof succeeds.
Thus Cruise cannot supply a same-artifact before/after pair even if its previous
ordinary evaluation succeeded. This is an observation defect, not the established
cause of lost physical viability. The live failure and these two producer paths
are baseline evidence; do not invent the missing previous Request from941.

Move the actual evaluated Request into diagnostic ownership on return. Retain
one last successful ordinary evaluation for every supported normal intent,
requiring Accepted, production authority and terminal_stop_certified. Attach its
Request to both terminal/final failure categories when the same intent/artifact
and an earlier decision match. This proves an observed ordinary evaluation,
not actual final publication; the existing publication ledger remains separate.

Extend the existing atomic first-failure writer with optional predecessor
evidence. Serialize each request's own clocks, observed wall grid and original
inspected source grid. Check ordering and immutable same-artifact identity.
Missing/invalid predecessor remains explicit and cannot discard the failed
world. Keep only one latest predecessor plus the existing finite queue buckets;
no new authority, planning worker, retry, event stream or callback I/O.

Native cases cover exact distinct before/after inputs/payloads, malformed and
missing predecessor, ordering and source mismatch, atomic first-event ownership.
Then package/build and a fresh dev2 observation must replay both actual inputs
without solving. Acceptance requires the previous result reproduces as accepted;
failure remains a new causal measurement, never race completion.

# Rear-peer stopping feasibility

Continue from4afa9948, autonomous MPCC completion still open. Baseline is the
fully tested observation repair, no live simulator/build. Same-world1629and
actual published1100are sealed in the preceding observation steering.

Earliest established failure: both terminal Stop references enter the rear
peer's CV full-body circle. The later Emergency is downstream. Original normal
horizon/continuation is clear. No predictor-only cause is proved; future GNSS
still overlaps complete stop, and includes D1's own later braking intervention.
No margin/radius change, assumed cooperative braking or future truth inputs.

Architecture gate: current A cannot linearize19; B/C/D/G require a front target
tube absent in this scene. Four independent fresh Stop solves reject dynamic
proof. These failures do not cover rear-aware physical terminal geometries and
cannot establish physical infeasibility. Add a bounded independent candidate
scan: terminal lateral targets -1.0..3.0m in0.1m increments, explicit linear
ramps of0/1/2/4m from current lateral state. Existing source course domain bounds
the profile. All164 candidates share exact recorded first publication interval,
maximum braking, seven-state Cartesian dynamics, actuator bounds, full peer
circle, unchanged CV model, measured/control prefix and swept wall reserve.
Source-copy offline observer exposes already built paths and outcomes. Record
every target/ramp identity and outcome; no scan candidate has production authority.

If a physical witness exists, establish a bounded canonical candidate producer
and immutable proof ownership before promotion. Prefer a structural scene
representation that handles all peers to another front-target exception. If
none exists, report bounded method failure/Unknown; inspect earliest coupled
planning predecessor and the separate stage19linearization failure.
Do not introduce another retry/hold/timing rule or tune acceptance requirements.

Acceptance: native before/after relevant invariant, one immutable proof/path
per published transaction, retired replaced candidate path, focused/package
tests,25package build, exact replay, then fresh single/dev2with same-run safety
and timing. Whole task also retains independent intents, dev3/dev4, gates and
submission evaluation. No production change before the witness/root cause.

## Confirmed separate tangent producer defect

All164profile candidates fail dynamic proof; do not promote another stop-reference
selector. Independent native probe of normal A's stage19failure records selected
progress18.805780563898864, virtualspeed3.2713509053908423,dt0.23937229491124654.
Their terminal progress19.588851337582255 exceeds immutable frame end
19.58885132565345 by1.19288046e-8m. Raw QP terminal19.588851337570393 was accepted
under unchanged solver residual. Selecting state and input separately does not
select a valid nonlinear transition. The strict frame evaluator correctly
rejects the malformed tangent; it must keep its1e-9domain check.

Offline only, intersect the tangent's virtual-speed box with the immutable
frame transition interval. The selected reference speed becomes3.271350855557159;
raw solver output and all QP constraints remain untouched. Normal A then solves
and passes wall/dynamic horizon proof, but terminal Stop still rejects D1
(-0.000656495m). This is a proved independent availability defect, not the
rear-peer failure's complete root cause. A normal authority is never granted by
merely changing this tangent. Promotion is limited to correcting this existing
adapter producer, not installing any of164uncertified terminal candidates.

Before/after native tests cover exact terminal support with variable stage time,
roundoff outside the model domain, immutable raw iterate and unchanged hard
bounds, plus empty joint input/domain rejection. Remove independent-only
selection for the progress/input transition; retain existing box selection for
other coordinates. Rollback4afa9948. No equation/schema/tolerance change, so
previous artifacts retain physical meaning; new solver candidate proof remains
mandatory. Build/package/replay plus fresh integrated run follow this local fix.

## Next bounded architecture: controls solved through rest

The added rear-peer QP experiment has8rejected outcomes: three normal methods
reach terminal rejection, one rejects nonlinear rollout, and all4maximum-law
Stops reject dynamic QP. Merely passing the missing peer to refinement does
not establish a fix and is not promoted. All164profile comparisons also share
the prescribed maximum-braking law, not arbitrary feasible acceleration.

Compare4offline full-horizon canonical trajectories with the original Cruise
speed/acceleration/steering bounds and original racing objective, adding exact
terminal rest. Future acceleration remains a solved variable rather than an
imposed maximum-braking sequence. Two topologies and bounded0/3SQP methods are
recorded. This is an explicit alternative terminal-policy hypothesis, not a
same-objective score comparison or a relaxation of wall/peer/actuator/rest
proof. The model, wall reserve, peer body/prediction, source/control prefix,
clock and horizon are unchanged. No production promotion until a full solved
and certified trajectory exists and its authority/receding successor is designed.

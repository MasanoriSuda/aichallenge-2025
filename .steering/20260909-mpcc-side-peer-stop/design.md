# Side-peer and Stop audit

Baseline b530883997e80cd78aa7e70bcd53bf2b6bcf93fc. The user authorised
autonomous completion and local commits. Overall acceptance remains open.

The rejected complete-body dev2 run loses authority at Domain1 decision941.
Its selected side peer is present in the physical world but absent from the
solver tube. The resealed world396 comparison supplies the same peer with the
physical oracle's constant-velocity prediction. It admits a physically certified
maximum-braking Stop with historical objective and exact corner support, while
the current zero-objective Stop fails dynamic refinement. This rules out a
physical impossibility claim for this model/world, not an integrated run failure.

Before another production change, save the zero-objective failed QP in its own
process and the full-precision accepted historical candidate in a second process.
Compare the identical failed QP using the existing row-normalized and internally
equilibrated policies. Preserve warm/cold cases, hard bounds, objective, physical
tolerance, iteration limit, world, model, time, and raw outputs. A solved QP is
not an execution certificate. The offline witness must not use publication-ledger
recording or enter a Store/publisher. No weights, margins, tolerances, iterations,
retries or authority changes are authorised by a successful comparison alone.

Then attribute numerical conditioning versus objective-dependent support/dynamics
generation, and implement the smallest evidenced producer repair. Selected side
observation must reach the optimizer coherently with its immutable world identity;
full multiple-peer coverage remains a subsequent acceptance obligation.

Rollback boundary is b5308839. User result/safety JSON and unrelated raw artifacts
remain untouched. No simulation runs concurrently with production edits/builds.

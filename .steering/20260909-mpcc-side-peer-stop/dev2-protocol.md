# Changed-input dev2 acceptance

One standard `make dev2` run after the b5308839 baseline and the evidenced
side-target, common-frame obstacle plane, zero-Stop numerical representation,
shared tangent and current-world Stop scope repairs. No repetition of the
unchanged rejected full-peer-body run. All failures and earlier successes remain.

Prerequisites: full 25-package build and all MPCC package tests pass. Seal source
patch, every changed source, native executable, GNSS library, map/config/image
and this runner before launch. Preserve the two user-owned result JSON files
and restore their exact bytes after shutdown, retaining generated replacements.

Use standard spawns, collisions enabled, no NPC/handicap/wall recovery changes;
the sole simulator overlay changes unlimited laps/timeout to six laps/600 seconds.
No model, weights, margins, bounds, timing or solver-setting changes during run.
No simultaneous source edit, build or heavy replay. Host deadline 840 seconds.

Stop after the first logged moving Emergency/Recovery in Ready/Start, active
Stuck action, both vehicles finished, or the host deadline. Preserve two seconds
after a failure for its complete snapshot. The ANSI-normalized monitor checks
the actual authority, not only the tactical phase. Missing result JSON is unknown,
never penalty zero or completion. Scope is local simulation, not submission eval.

Inspect each vehicle's outcome, wall/peer proof, final publication identity,
certified Stop over multiple ticks and subsequent normal restart, Follow and
complete signed ShiftOut/Pass/Return episodes, callback/receipt/source time.
Unobserved episodes remain untested. A failed trial is revisited only after a
new sealed causal input or structural repair, never by relaxing acceptance.

# Common terminal programme candidate

2026-09-11 JST. Baseline/rollback4210fa89. Full M4–M6are unfinished.

Dev2-r21D1decision953/source261 rejects an applied wall sample at11.739999760,
although its nominal physical trajectory passes. Previous952/source270 is a
different actual publication and its original proof passes. Native r95keeps
the final reference arm and reproduces this exact rejection.64finite sampled
responses are clear; that alone does not prove the full response population.

The two existing terminal generators track either the solved lateral path or
the track centre while braking. Both are heuristic choices for the Stop
feasibility obligation. Neither is a necessary condition for body rest, wall,
peer or Follow clearance. In this scene they create a changing future steering
sequence whose full receiver-response envelope rejects. The producer has no
constant serialized steering candidate, although the same first normal packet
followed by that programme can satisfy every existing physical condition.

R100diagnostic preserves the exact first normal packet, then uses the existing
minimum acceleration and the same steering word from the next publication
interval through rest. Original input history/profile/model/wall/full CA1peers
are unchanged. The full numerical population reaches rest11.874999760with
wall/state clear and minimum peer0.797839m. This is candidate evidence only.

R101isolated source variant passes that candidate through the existing nominal
9state trajectory, current-world wall/dynamic/Follow proof, applied all-response
certificate, production adapter, Stop materialization and same-epoch join.
Exact r21D1953accepts9.89ms, r20D1964accepts9.98ms, r19D21071accepts8.08ms
(minimum peer0.00518309m). These are local replays, not live25ms or race acceptance.

Add this programme as the first terminal feasibility candidate only for a
normal artifact with the required applied-input context. Its first packet is
the already selected immutable normal packet; it cannot change that command.
Use the existing build_stop_program and every existing physical gate. On
rejection retain the distinct solved-path and track-reference candidates.
An already complete solved Stop suffix is never replaced or shortened: its
delayed braking may be necessary for a rear peer. No second normal authority,
direct low-speed control, unproved hold, margin/rate/budget/grace or model change.
This changes the terminal candidate population, not the acceptance conditions.

Record the selected programme explicitly in proof/telemetry and accumulate all
attempt costs. Retire the assumption that only the two lateral tracking laws
can discharge this obligation; no old normal authority is re-enabled. Tests
must bind the actual first word, full rest, unchanged world and materialized
programme, preserve delayed solved Stops and reject absent/unsafe input.
Then build/package and sealed native cases, freshdev2-r22and all remaining
M4–M6. The new candidate does not establish recursive or race acceptance.

## Other audited paths, not promoted

R97aborted while inspecting a failed programme preparation; r98records it.
Materializing previous952/source270then joining current953has steering-step
rejection9. R99shows a first-to-second step0.0473783rad against0.0257925rad:
the current publication correction is followed by a later old schedule word.
The unmodified publication-clock suffix itself still rejects the wall at
11.704999760. Thus merely retaining the prior programme does not solve r21.
Do not promote that rejected experiment or claim the actual953request had
this preparation failure. Actual953source261failed the applied wall gate.
Publication retention/clock semantics remain open, separately from this repair.

Same-world A, B-left, C-left, D-left and production-left nominal candidates
pass; right-side arms reject. Complete-restY passes at14.0421ms. None grants
an applied certificate or proves all-method infeasibility. Record every arm.

## Verification

The production candidate passes 83 focused tests, build r45 (26 packages),
and package r38 (2434 records / 62 groups, zero errors/failures/skips).
Fresh current-header drivers r102–r104 accept all three sealed moving failures
through complete certification, production, materialization and same-epoch join.
Existing positive r17 and signed r16 pass; the actual r18 history gap remains
rejected. Complete solved and delayed-braking Stops remain unchanged.
The numerical kernel is unchanged; enclosure-r14 remains its broad regression.
See [evidence](common-stop-candidate-evidence.json). Fresh dev2-r22 and M4–M6
remain required. Isolated timings do not establish live 25ms acceptance.

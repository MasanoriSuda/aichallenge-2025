# Applied-input tube integration

2026-09-11 JST. Continue M1–M6 without routine approval. Parent shared model
`2fc31bf7`; rollback remains that commit until the complete authority slice is
validated. This supersedes the diagnostic transcription boundary, not the
recorded failed runs or original physical requirements.

## Evidence and interpretation

Scalar body arithmetic is shared and bitwise equal to the frozen old producer.
Source-stop-r6 encloses both channels from each original source epoch, then one
common future float32 Stop program. All four earlier worlds reach full nominal
body rest with original wall/peer checks. The previous r4/r5 divergence arose
first in tire interval wrapping: a fixed convex-hull invariant fails at step4
in the saved regression. Monotone opposite-corner bounds from the same shared
tire formula repair the numerical method;160,000scalar checks and the complete
existing enclosure suite pass. No tire/body parameter or wall margin changed.

Application-r5 uses five observational calls, with all3,608original CIL methods,
branches and exception targets otherwise equal. The declared100ms steering
packet-age candidate fails startup(max134.715637ms); do not exclude it. All1,865
complete steering intervals are below the separately reported250ms hypothesis.
Longitudinal434selections/432intervals pass250ms(max192.562235ms). These support
an empirical250ms interpretation, not a transport bound.100ms is not adopted.
The same run fails D1decision966/source438: retained steering-unreachable and
new Stop course-frame-unavailable; source414/decision964 is an unpaired accepted
historical input. This is not a successful race or a pure peer-only failure.

## One program, nominal solve and possible applied trajectories

The canonical nine-state solve remains the only normal optimizer. Its complete
solved controls determine the first serialized packet and one common future
program, including a certified full Stop when needed. A physical certificate
binds both the existing nominal solve and every enclosed applied-input response
to that same immutable program. A per-response feedback choice cannot provide
the certificate. The nominal objective does not score different receipt arms.

Receipt uncertainty is represented as all causal packet values in the declared
age window, with separate steering mechanical delay. Convex input ranges may
overapproximate those values. Equal publication epochs preserve every packet in
publication order; the nominal point predictor may choose the last one, while
the uncertainty model must retain the earlier still-possible values. A clock
regression invalidates that history. Missing history and nonfinite/unsupported
contexts reject; never insert an unobserved zero or fitted acceleration.

The initial empirical body state and every component/source epoch remain
explicit. Propagate the original source through actual history to publication;
do not start from an immediate-braking point and call it uncertain. Body/model
error and physical contact are still empirical limitations, separate from the
input window. Coordinate ranges use a fixed original body heading. Original
footprint, wall clearance, peers and peer temporal motion remain unchanged.

The program may repeat its final nonpositive packet each publication period
until every admitted body state is at exact nominal rest. That duration defines
the materialized Stop horizon; no expiry extension or grace based on a label.
Preserve a solved delayed-braking Stop required by a rear peer. Do not replace
every Stop by immediate maximum braking or drop a valid solved suffix.

## Implementation boundary

1. Move tested interval arithmetic into the vehicle model library with a typed
   input-program/profile/tube interface, remove the diagnostic implementation,
   and reuse it in source-world replay. Validate packet windows, duplicates,
   invalid/missing histories, both channels, source-to-now and full-rest tail.
   This library slice has no runtime authority by itself.
2. Bind profile, source epochs/history, common controls, all-response rest
   horizon and range samples into immutable fingerprinted proof data. Cover
   candidate/fresh/retained/source-free Stop, materialization, async adoption
   and final serialization. Retire replaced nominal-only application claims in
   the same authority slice. No shadow result may execute.
3. Reconcile actual publication epochs with the existing nominal130ms control
   origin. Certify the actual common serialized program explicitly; never call
   a now+n*25ms steering program identical to a control-origin trajectory that
   begins130ms later. Preserve all original hard state/input and intent checks.
4. Validate frozen failures and early normal-plus-Stop candidates, then run the
   current build/package and live Stop/rest/restart. Range/proof alone currently
   costs9.3–17.9ms; callback acceptance is open. Share work or optimize measured
   arithmetic before accepting25ms; do not increase its budget.
5. Complete remaining M4–M6 campaigns, gates and same tar/image/eval, then final
   evidence/docs/local commits. No current checkpoint claims that completion.

## Library checkpoint validated

Step1 is implemented. buildr37/testsr30 and library-source-r2 pass. Equal-epoch
publication loss and whole-window coverage each have frozen old-library negative
reproductions. Clock bridging uses counted native steps with explicit duration.
Monotone tire refinement and full numerical implementation live only in the
production library; the diagnostic header delegates. Steps2–5 remain open.
At that library checkpoint the controller had not adopted the profile or tube.

## Common-program certificate slice (in progress after95180a1c)

The old terminal proof records a nominal point trajectory starting at
control-origin, while actual packet receipt can still apply earlier inputs.
This is the earliest input obligation exposed by saved955/956andapplication
selections; successful earlier held-Stop diagnostics are not its fix. The
producer change samples one common serialized program from the existing
current-world Stop controls, preserving the selected first packet and any
solved delayed braking. A continuous future steering reference is explicitly
resampled, so the original point trajectory is only nominal provenance.

An immutable additional certificate carries nominal source identity, complete
current request/history/profile, exact common program and all-response-rest
tube. Shared footprint arithmetic checks original wall clearance, every full
CA1peer circle across each interval, actuator state bounds and Follow gap.
Follow progress uses all possibly-nearest sealed polyline segments, rejecting
missing map support or possible crossings; nominal semantic constraints remain
separate required checks. The same program can be re-anchored for a new
publication only after a new current-world proof, never by renewing its age.

The slice was initially inactive in controller requests. Its promotion requires
all Stop materialization/retention, async adoption and final packet consumers
to retain this object/program and all-response rest horizon. The retired path
will be nominal-only Stop execution for live profiled requests; old snapshot
readability does not authorize it. Tests must reject changed future controls,
world, original history, missing coverage, collision and first-packet mutation.
Rollback before promotion:95180a1c. No margins/model/solver/rates are adjusted.

2026-09-11 continuation: required applied certificates now reach command candidate
and production adapter in the working tree. Materialized Stop data binds the
original program/history/profile/rest horizon with a new problem-context field;
it stores no parent plan pointer. A native program replay preserves delayed
braking and runs the same held packets until every-response rest is covered.
The live builder now requires the explicit empirical profile and the final
ROS float fields are checked before publication. Full build/package, source
replay and dynamic acceptance have not yet validated these working-tree changes.

A final-boundary review found another first-input mismatch: the prospective
producer rounded after applying gain, whereas the actual ROS pipeline rounds
physical steering first and calibrated steering again. The already existing
20260827counterexample(-.271520636, gain1.435) distinguishes these values by1ULP.
A common lower-level serializer now owns both the publisher contract and
prospective/program/Stop producers. Wrong single-round packets must reject.
The input tube also covers the complete first publication interval when already
at rest; stopping its proof after5ms would not cover a25msfirst packet.

## Integrated candidate local validation

Live requests now require the complete certificate; materialization, remaining
Stop horizon, async compatibility and final packet publication consume it.
All2425package records/62groups pass afterbuildr39(26packages). Initial fixture
producer failure is archived. Frozen old/current serializer replay gives the
expected opposite outcomes. Three exact saved failures and unpaired historical
requests remain rejected; old packet mismatches are not edited away.
These local results do not close M4-M6 or25ms acceptance. Next frozen dev2-r16,
first-failure attribution, then the remaining same-HEAD campaigns and submission.
See applied-integration-evidence.json. Rollback is95180a1c.

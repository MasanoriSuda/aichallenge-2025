# Exact source failure after a post-motion final loss

2026-09-13. Authorized autonomous continuation; baseline/rollback `7b44f455`
(production `ec8e822b`, algorithms/model `8d9810e0`).

Single-r8 and R589 establish a correct final1052 RestExpired. The latest complete
failed source622 is at11.794999736, before later accepted652 and final14.299999680.
Old intent/side/stage/outcome capture slots hide exact newer solver failures.
R590 confirms old A failures; target-dependent B/C/D/G cannot cover this
ordinary target-free Cruise. Earlier failures must not stand in for the actual
post-loss fresh source. This is a missing observation, not a new solver fix.

Freeze a diagnostic anchor exactly when a stationary final due/active failure
is admitted after authenticated normal motion. It contains final control
decision/clock and the prior moving normal publication witness. Propagate it
through owned MPC async clones and the existing optional received-body payload
on the newly built solver source. Preserve source and current control IDs as
distinct namespaces. Clear the live anchor on backward/nonfinite control clock.

Preserve every original source recording bucket/path. Also record the first
post-loss failure in each existing intent/side/stage/outcome bucket, separately
for the two existing native initializer policies. Keep exact failed QP, original
warm start and outcome; physical failure without a failed QP keeps its entire
immutable source/world. Additional names have a fixed post-loss/initializer
suffix, never per-decision/reset keys. All writes remain on the existing solver
worker. No additional authority, thread, runtime parameter or indefinite retry.

Optional anchor validation must reject future/mismatched control epochs,
stationary/unsourced prior publication and malformed serialized data. Invalid
optional metadata leaves original replay evidence usable. This anchor, like
received queues, is outside model/interaction/solver/certificate fingerprints.

Acceptance: old/new native capture regression including both initializers,
exact QP/warm-start roundtrip, original bucket preservation, invalid metadata,
legacy replay and fingerprint stability. Source placement/clone/clock contracts;
build123/package110 and fresh original wall/real deadline/final1052 replays.
Then local commit and one bounded original-settings single-r9 to obtain the
missing upstream input. Assess that actual source before any producer change.
Full Stop/restart and M4–M6 remain open; expiry itself justifies no bypass.

## Validation and review

R591/R592 retain fixture setup failures: Cruise cannot inherit Pass execution
side1, and replay-world prefix times must match the declared control origin.
No production validity check was relaxed. R593 uses the same old/new objects
with a valid resealed fixture: old3 fail/new3 pass. Both original and post-loss
source captures remain, per initializer. QP/primal/dual/rejected iterate and
physical-only source roundtrip; anchor metadata never changes fingerprints.
Invalid/malformed anchor keeps the original source usable. An I/O error remains
visible and does not consume the additional slot before a successful write.

Source106 passes; its initial function locator mistakenly selected a callsite,
then was corrected to the full definition signature. Build123 all26,
package110 all2618/66 pass with no errors/failures/skips. R594 freshly compiled
four-case replay retains original2wall,1actual deadline and1052RestExpired.

Review: only an admitted post-motion final pair creates the anchor. Clock reset
clears live provenance; immutable async clones retain their own historical
anchor. The solver-source builder copies bounded optional received queues
before adding the shared anchor, so later live changes cannot mutate queued
source evidence. No new thread or solver/normal fallback; full original buckets
and failure outcome names persist. The source worker performs the added I/O,
so single-r9 is a diagnostic observation and must still measure live costs.
[Results and hashes](post-loss-source-observation-evidence.json).

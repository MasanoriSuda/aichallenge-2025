# Programme starting-domain coverage

2026-09-12, baseline/rollback f8a380f6 (production4495ab3e). M4-M6 remain open.

R56 earliest preD1decision530/source14/index4 and stopping postD2decision1268/
source742/index4 require a complete current proof, since the existing independent
starting-domain producer covers index0 only. R334 reproduces both original
pre/post outcomes after exact four actual sends and history pruning. The costly
current proof is a contributor; D1 also has about18ms unexplained off-CPU/MPC time.
That time is Unknown and is not repaired by numerical coverage alone.

R332 persistentA accepts both original sources; B/C/D lack Track/Cruise and are
inconclusive. R335 older snapshot reader failed on absent optional transactions;
R336 compared explicit command windows but omitted the repeated braking tail in
its generalized time interval. R337 includes the original full-rest horizon:
whole source-derived state/time domains, with or without additional pose symmetry,
contain all six frozen prefixes (R51/53/54/55first, R56D1/D2index4) and pass whole
fresh/source world and rest checks. Independently reboxed25ms layers pass worlds
but miss D1lateral pose. Originalpointfuture reanchoring/reuse remains rejected.

Selected bounded production slice: independently propagate a whole starting box
seeded from the raw state plus the original complete source tube, for every start
time between original first packet epoch and original proved rest. Retain existing
first-prefix pose symmetry but do not add full-future symmetry: R337 both pass,
so avoid the additional unused enlargement. Every newly declared state/time/corner
is propagated to full rest; old samples supply only the starting-box proposal.
Explicit new numerical request/API binds the original rest horizon. The original
first-window API and its strict validation stay unchanged.

The worker replaces its index0-only domain with this single programme domain;
source certificates with pending prior packets remain unsupported. The current
short prefix uses actual retained history plus the exact remaining suffix, while
the independent future theorem retains the ENTIRE original programme, including
past packets' delayed input memory. Original actual-prefix authentication must
precede this join. Private proof binds original certificate, current decision and
suffix index. Original source IDs, nominal appointments, individual25ms pre/post
windows, slew, original rest expiry, context/epoch/world/Follow checks stay strict.
No old CurrentPhysicalProof is relabeled: it remains a separately complete proof
when the new domain cannot cover the current population. No new normal producer.

Regression checks: native scalar/corner enclosure at late starting times and
positive delayed-input expiry; invalid/time/horizon/hash rejection; exact current
prefix parity after multiple authentic actual sends including repeated tail;
missing/wrong source, index, history, current world, generation and original
pre/post deadline rejection. Repeat six actual-library frozen replays, then build,
package tests, quality/source review, local commit, standard dev2 with additional
minimal phase observation if the unexplained critical interval remains causal.

Risks: whole-state/time boxing can reject otherwise safe curved/high-speed worlds;
worker still computes original proof plus independent proof (~8-10ms additional in
R337 isolation). Combined short-source-prefix/domain producer remains an unpromoted
alternative, requiring explicit certificate ownership design. Neither R337 nor
this local slice establishes moving race acceptance or resolves unknown off-CPU.

R338 numerical tests8pass, including all256 starting-box corners at three late
starting times, physical footprint containment and delayed positive packet memory.
R339 dispatch146/148pass: the proposed unrestricted whole-state/time Cartesian
product fails the existing Follow gap domain check (ordinary complete proof still
passes). Preserve the original first-window domain for sources carrying a Follow
target; late Follow remains complete current reproof. This slice expands domain
coverage only without that temporal target constraint. No Follow gap requirement
or prior regression assertion is weakened. A future time-dependent Follow domain
needs a separate comparison. The other failure is the new repeated-tail test's
old helper requesting a finite solver-stage packet beyond its horizon; the test
now advances actual clock/history and binds the original repeated terminal packet.

Add bounded observation-only checkpoints at scheduled mailbox, request/proof,
admission logger, failure-capture and candidate binding boundaries. Fixed20slots
record steady/threadCPU and Linux thread voluntary/involuntary context-switch
counters; only failed/slow callbacks format them after the publication boundary.
These counters cannot identify a particular scheduler/mutex by themselves. No
control/admission timing threshold reads this data; no log suppression, scheduler,
rate, affinity, timeout or budget change. Observation promotion boundary: retain
only while R56unattributed phase remains Unknown, then review/remove detailed
checkpoints after a causal result.

Buildr96all26, package testsr84all2564records pass. R340six actual-library
replays adopt programme domains with exact current full-prefix parity and original
source/actualwindow outcomes. R56index4 current medians: r56-d1=0.626737ms, r56-d2=0.760795ms.
[Evidence](programme-domain-evidence.json). Standardr57 remains required.

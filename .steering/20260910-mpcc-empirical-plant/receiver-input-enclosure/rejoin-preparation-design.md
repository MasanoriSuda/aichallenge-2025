# Rejoin numerical preparation design

2026-09-13. Baseline49cecda1, runtime7468cf46. Authorized implementation and local
commits, no push. [Paired evidence](rejoin-preparation-holdout-evidence.json) and
[original source-state comparison](nonlinear-source-state-design.md) retain the
failure inputs, current/longer seed negatives, and original physical requirements.

The current Rejoin source gets only the current-steering numerical initialization.
In independent537and548worlds that cold source hits the wall. A coherent native
steering-before-drive initial tangent allows the same free nine-state problem to
satisfy original source boxes, swept wall, peers and Stop. This is a candidate
initialization defect at this boundary; it does not establish a Mission lifecycle
root cause or remove the separate missing current-envelope/physical-observation
issues. No raw primitive may become a published control or certify itself.

Use the existing two-owner numerical population for target-free, side-neutral
Rejoin as well as Cruise. Cruise retains reference-rest then current-steering.
Rejoin gets steering-before-drive then current-steering, with both immutable IDs
reserved before any solve. One attempt per member, distinct existing solver owners,
full physical/certified Store admission, and the original second member remain.
Do not append a third owner, change solver iterations or broaden normal authority.

The new numerical policy derives steering direction from the first future lateral
reference relative to semantic initial lateral state; use the corresponding original
cumulative steering bound (or current steering for zero difference). Hold zero
acceleration while that steering target is reached under original per-stage rate
bounds, then seed the two drive stages fixed in the paired537/548comparison, followed
by original lower acceleration. Virtual speed is zero within original input bounds.
All nine tangent states advance through the native kernel. Nonzero initial speed,
tire, lateral velocity and yaw rate are preserved; zero acceleration is not asserted
to mean a stationary vehicle. The pattern selects numerical tangents only: original
QP state/input references, weights, linear costs, bounds and each control remain
unchanged and free. Any infeasible final trajectory still rejects. The current seed
remains the alternative when this tangent fails.

A forward objective is necessary for this Rejoin alternative. Target/dynamic/braking
exclusions stay intact. Direction is a numerical proposal; hard world proof owns
physical acceptance. Mirrored lateral errors, moving initial state, exact phase
transitions and unchanged cost/constraint data need focused native tests. The policy
ID must be serialized/fingerprinted and warm starts cannot cross it; unknown values
remain rejected. [Replay enum compatibility](../../../docs/interface/mpcc-snapshot-interface.md).

Before production code: failing adapter policy and Rejoin population tests; then
implement the source policy and truthful runtime label. Extend existing snapshot
round-trip/unknown-policy coverage, replay537/548and existing accepted/failed sources
with actual new code, run focused/source tests, build136/package123, commit and
single-r21. Do not call offline initialization or one live run race acceptance.
Rollback is the source slice after49cecda1. Current/retained speed, late missing
geometry input, physical observations, r80D2actual-send and all M4–M6 remain open.

R750 old adapter/population regressions fail. R751/R752 preserve the new test's
incorrect offset sign; corrected existing equality convention passes R753 all208
native tests, including policy2 round-trip/unknown rejection and warm isolation.
Source118, build136all26 with unchanged sources, Summary: 2645 tests, 0 errors, 0 failures, 0 skipped.
R754 actual repository policy accepts537/548 with original hard proof; original
members keep wall144/720. R755 retains430solver/622wall, and527 preparation rejects
current physical wall contact while its original solver rejection is unchanged.
The R754 inapplicable-input abort and R755 optional-capture-detail assertion are
retained; R756 validates actual case results without rerunning or relaxing proofs.
[Validation and review](rejoin-preparation-validation.json). Fixed single-r21 next.

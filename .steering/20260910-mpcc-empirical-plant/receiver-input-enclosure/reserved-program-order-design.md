# Reserved programme producer design

2026-09-13. Baseline0685b076; runtime40d1664b. Authorized autonomous implementation,
full verification and necessary local commits; no push.

[Audit and sealed comparison](reserved-program-order-audit.md) establishes the
actual immutable source, wall failure, unchanged programme/domain fingerprints,
and timing contribution. R739adds five fixed contexts: zero-prior495/888 unchanged;
reserved777/863same accepted fingerprints; failed1010still rejected after the same
five attempted profiles. An accepted source for863still does not admit its late
final publication. All original physical, clock and semantic guards remain.

The producer puts a rejected long terminal programme ahead of the accepted span
already defined by reserved pending publications. This consumes the calculation
budget before the same certificate is ready. Repair that dependency order, not the
wall rejection or expiry detection. R737compares four existing programme orders
for one solved source; it is a downstream scheduling comparison, not a replacement
for the Mission/SQP A/B/C/D architecture gate. Normal nine-state authority, source
population, formulation and backend remain unchanged.

For an eligible reserved source, first try the already defined reservation span
only when it is shorter than the source-horizon proposal. If that proof rejects,
try the original full-horizon programme, then the existing bootstrap candidate and
existing lateral-reference alternatives. Never repeat a member or enlarge the
finite population. Share the exact original interval-count calculation between
proposal selection and construction so equal/longer spans do not become new dummy
attempts. Zero-prior order, solved Stop, rear-peer feasibility and every complete
nominal/applied/domain/current/final proof remain. Do not extend time windows,
reduce model uncertainty, change speed limits or ignore failed candidates.

Before production code, extend the existing intermediate-before-peer fixture to
require one proof attempt: it already verifies full applied rest/history/identity
and a blocked negative. Preserve its old deterministic failure. Then implement
producer ordering, run full retained/scheduled regression suite and source tests,
replay eligible1223and all five R739contexts with actual compiled code. Full
build/package and a fixed-commit single-r20 must confirm practical scheduling.
No wall-time assertion substitutes for the original final publication guard.

Rollback is the next source slice after0685b076. Remaining source/current velocity,
physical measurement, source preparation, r80D2actual-send and M4–M6 are unchanged
obligations. Single-r19globalgameStart is observed; vehicleStart/laps are not.

R740old fixture deterministically needs2proof attempts instead of1; existing applied
Stop, history, identity and blocked-peer checks retain their assertions. R741all166
retained/scheduled tests and source118pass. Source-horizon interval count is shared
using the selected command cursor, so stage advance cannot change the comparison
boundary between producer and constructor. R742actual archive preserves six input
classes and exact accepted programme/domain fingerprints, including original1223
now24.595+18.848ms. Build135all26, sources unchanged; Summary: 2643 tests, 0 errors, 0 failures, 0 skipped.
[Validation](reserved-program-order-validation.json). Next fixed-source single-r20.

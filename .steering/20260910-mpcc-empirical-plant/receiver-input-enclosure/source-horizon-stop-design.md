# Stop programme within the remaining solved source horizon

2026-09-11 JST. Baseline/rollback749a871c. M4–M6 remain unfinished.

Dev2-r23 first loses authority at D1decision1018/source316,2.90m/s,WP35,
wall1789073210.216376634. Previous1017/source342 is a different actual
publication; the recorder correctly rejects cross-association. Original
captured request now12.374999723 finally rejects peer d2 at13.519999723.
The first constant-steering immediate Stop instead rejects peer13.889999723.
D2 had no moving override before teardown. Full callback127.363ms,
primary89.260ms, alternate19.322ms, published Stop join9.111ms. Replay r109
reproduces the exact final reason/time. No run acceptance or25ms claim.

Expanded geometry-only diagnostic r106 cannot certify the common programme:
128 separating directions still leave minimum−0.07196370m, rest13.934999723.
That driver retains its frozen earlier libraries; only the unchanged numerical
kernel and geometry comparison are used, not its old production gate.
R111 varies terminal braking through six original-bound values; all final
selections reject. R112 records the common arm itself for18combinations of
braking and one-period reachable steering: no accepted programme.

R113 incorrectly omitted periodic copies of the first packet. Its delayed
programmes reject InvalidActuation before physical feasibility; those trials
are inconclusive, not evidence against delayed braking. R114 writes every
original25ms publication. Fixed programmes beginning maximum braking after
0.2/0.4/0.8s pass every nominal/applied/production/materialization/join gate;
0.025/0.1s do not. These are diagnostic future control sequences, not runtime
waits, retry/grace rules or unproved holds. The original first normal command,
input profile, world, control limits and physical requirements remain fixed.

R115 derives the prefix from the current solved source's remaining reconstructed
normal horizon0.294234972s. Rounding down to existing publication periods
gives0.275s (11positive packets then braking;12programme entries). Its old
brake_after_intervals=1trial label is unused; the emitted programme timestamps
are authoritative. Exact source316 is loaded and identity-validated. The
full response population peaks3.232361573m/s against the original remaining
source speed ceiling11.111111111m/s. Full certificate, production,
materialization and same-epoch join pass12.95837ms; rest14.239999723,
minimum peer0.169652688m. This remains isolated evidence, not live acceptance.

The producer currently admits immediate maximum braking and two steering
references as normal terminal hypotheses. For this rear approach, immediate
braking is an unnecessarily narrow feasibility condition. Add a finite
constant-steering programme whose positive prefix is bounded by the existing
remaining solved horizon, then applies existing minimum acceleration to rest.
No fixed delay setting, new normal controller or changed hard bound.
Completed solved Stops remain untouched, including already necessary delayed
braking. Programmes require dense original-period words and immutable identity.

Before promotion, bind the minimum remaining solver-source forward speed
ceiling into the nominal and full applied proof, carry it into materialized
Stop provenance and recheck it on every successor. Ordinary legacy programmes
retain their old format; bounded-prefix provenance must use an explicit new
schema and fingerprint so older readers fail closed. Every original gate
still applies. The generator needs the exact identity-matched solver source;
missing bounds cannot invent a speed limit or enable this new candidate.

Try the longer programme first only when a rear peer is predicted to reach
the current vehicle footprint during a nominal stopping-time estimate. Use
one step of the shared native model for that proposal-ranking estimate and
the original CA1 circle/footprint, with no added threshold or model parameters.
This estimate supplies no authority. Otherwise prefer the immediate programme.
Every chosen programme still needs complete certification; the distinct
alternative remains available, with all attempt costs recorded. Verify the
ranking and cost on sealed inputs before fresh runs. No cached duration or
historical grace state is introduced.

Required tests: actual first packet and dense schedule; prefix bounded by
remaining source horizon; exact source/bounds identity; full-response speed
ceiling rejection and proof tampering; v1/v2 provenance round-trip and invalid
version/ceiling; materialized multi-tick Stop retains the bound; existing
complete/delayed Stops, missing-history, wall/peer/Follow negative cases.
Then focused/full package/build/current-header native replays, a local commit,
freshdev2 and remaining M4–M6. Source observation/publication clock agreement,
peer evolution and runtime25ms remain open until measured.

Same-world persistentA nominal passes21.2774ms; B/C/D/G cannot form a current
canonical target and are inconclusive. Complete-restY first solver-rejects
112.151ms, then passes22.8933ms. Preserve both and do not infer all-method
physical infeasibility. All trials and revisit conditions belong in the registry.

The source horizon bounds proposal duration, not authority duration: the
normal continuation proof can retain only its first publisher interval. The
new terminal programme independently proves every later state through complete
rest. No unproved part of the normal suffix supplies execution authority.
The regression checks both ranges and rejects absent/truncated terminal proofs.

## Verification

Build r48 failed because the solver source was only forward-declared; include
its existing definition. R49 passed26packages but exposed a missing optional
initializer warning; initialize it explicitly. R50 passed26packages. Package
r40 found one new test comparing the terminal prefix to the normal authority's
truncated25ms array, instead of the original remaining source horizon. The
separate full terminal proof and both materialization checks already passed.
The test now verifies the normal25ms limit, source-horizon limit, complete
terminal/applied coverage, and rejection of missing or truncated terminal
proofs. No production gate, tolerance or existing test was relaxed. Automatic
approval review initially rejected this correction as an apparent test
relaxation; after inspecting the two distinct proof ranges and adding the
coverage/rejection assertions, re-review approved it. No approval is pending.
Build r51 passes26packages/19.4s; package r41 passes2441records/62groups with
zero errors, failures or skips in29.14s. Current-library r116/r117 original
snapshot replays and frozen sources are in [evidence](source-horizon-stop-evidence.json).
Freshdev2-r24 and remaining M4–M6 are still required. No native replay duration
is counted as a live25ms acceptance result.

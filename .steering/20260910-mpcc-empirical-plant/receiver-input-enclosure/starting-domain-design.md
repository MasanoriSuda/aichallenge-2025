# Independent starting-domain comparison, baseline dbbe5fe6

2026-09-12 JST. Diagnostic only; no production authority or parameter change.
Current full physical proof is necessary because new sensor states are outside
an old point population. R193/R194 reuse remains rejected. R316 rules out replay
vs production-library linkage as the cause of the 7 ms isolated / 11 ms live CPU
difference. Full future integration still runs synchronously on every dispatch.

Compare a genuinely separate initial-domain theorem, not observation tolerance:
for every initial body state in a declared box D and every starting clock s in a
declared interval T, independently propagate the unchanged native model under
all original packet/receiver windows through full rest. Bind D, T, original wire
programme/history/model/profile and complete body/corners/rest to a new identity.
A fresh dispatcher would first recompute the short original-observation-to-now
population using actual history. It could use this theorem only if the entire
fresh population is in D, now is in T, all original actual prefix/context/source
checks pass, and the complete future enclosure passes fresh wall/peer/Follow
checks. Final actual pre/post window and source guards remain. Outside D/T is
rejection, never a widened measurement tolerance. D is a certified candidate set,
not a claimed sensor error bound; covariance supplies no hard safety guarantee.

Use now as the domain's integration origin. This preserves the current kernel's
variable short prefix followed by maximum_step_sec future steps. Starting from
a different raw sensor epoch with an arbitrarily rephased native grid is invalid:
the discrete tire integration is not an exact semigroup. Fresh short-prefix
body and corners must match the existing complete predictor at now. Membership
across different coordinate frames needs outward rotations and rigid corners;
no translation/rotation of an old future certificate is allowed.

For relative future interval [j*h,(j+1)*h], query all original admissible inputs
on [T.lower+j*h,T.upper+(j+1)*h] with outward arithmetic, then propagate D using
exact native h. This over-approximates all starting epochs. Initialize rigid
corners anew from the entire body box and original footprint, not from an old
point corner proof. Full rest must hold for all members, after all positive
packets have expired even for the earliest start. Recheck world over the whole
absolute time interval; fresh-world checking may drop strictly past intervals,
but must retain every possible future state/time. Follow target minima need the
same original branch and complete time range.

First bounded diagnostic uses each captured current population as an oracle D,
first with exact current start time, then with the full original 25 ms window.
This is feasibility/cost evidence only: the future observation was unavailable
to the original worker. No prediction-quality, worker availability or production
permission follows. If this minimal set/time box cannot prove the same current
world/rest, reject or redesign before implementing dispatch. Subsequent work
would require a causal worker-side D proposal, exact fresh short-prefix membership,
independent hash/type, all state/time/world negatives, source/async ordering and
retirement of replaced paths in one bounded production slice. All are unimplemented.

Completed bounded comparisons:
- R317 exact fresh oracle box and full25ms starting-time interval independently
  prove all three R51/R53/R54 worlds/rest. Short-prefix body and rigid-corner
  bounds match the complete current predictor exactly; not causal seed evidence.
- R318 proposes D from the original source tube over the original publication
  window, then recomputes every future range independently. R51prefix is inside;
  R53Y and R54X fail strict membership although each proposed set proves rest.
- R319 composes the frame rotation directly, preserving XY dependence instead
  of boxing in world axes and rotating back. This admits R53without changing D.
  R54still rejects the narrow box. A second causal proposal includes original
  source ranges from its observation through the same original deadline. It
  changes the candidate set, independently proves the larger entire set, and
  retains exact membership. All three now pass. No covariance/observed maximum
  or new tolerance is interpreted as an error bound.

R319source-history proposal: independent full-window computations7-8ms, current
short-prefix0.10-0.25ms and world-only recheck0.46-0.59ms in isolation. The source
proposal/history/programme/profile and numerical propagation need no future
observations. This diagnostic still checks the full enclosure with captured fresh
world; an implementation must separate worker numerical evidence from dispatcher
current wall/peer/Follow checks. No worker availability or live authority is tested.
The existing CurrentPhysicalProof stays in production until a typed integration
is reviewed and tested. [Sealed evidence](starting-domain-evidence.json).

Next implementation boundary: a separate immutable numerical StartingDomainTube
with its own D/T/frame/model/profile/programme/history hash; a typed short current
prefix; strict whole-prefix membership and fresh complete-world checks; retained
original source/generation/ledger/index/actual final guards. A worker numerical
result alone must never mint DispatchCandidate or published authority. Preserve
full current proof on any case outside the new theorem until explicitly retired
by a complete equivalent path; no grace/held-positive packet/new fallback. Test
invalid values/frames, time boundaries, input coverage/positive expiry, changed
programme/model/ledger/context, outside-state membership, full rest, wall/peer/
Follow failures and native scalar/corner enclosure. Then build/package tests,
exact first-failure replay and fresh standard runtime.

# Intersect independent enclosures of the same native map

2026-09-11 JST. Baseline/rollback c87b9fa6. Full M4–M6 remain open.
Dev2-r20 first moving D1Emergency is decision964,2.29m/s,WP33. The exact
captured request evaluates source260 and rejects an applied wall sample at
12.254999750, from publication11.144999750. Previous963/source277 is a
different request and remains accepted; its cross-source association is marked
invalid in the recorder. It must not be relabeled as the same candidate.
Actual published277 also lost its own current-world join in the live trace.

Native r90 reproduces both separate requests and checks the actual final
terminal-reference arm. D1964nominal proof passes but its applied enclosure
touches a wall. The64sampled independent-channel trajectories all stop without
wall contact and lie inside the original bounds; this cannot prove every
possible response safe. The rejected box has xwidth0.8865m/ywidth0.4042m;
the sampled hull has xwidth0.6664m/ywidth0.1568m. Source/program/model/profile,
wall and peer requirements remain unchanged.

Diagnostic r89 initially obtained the nominal proof by disabling the applied
gate. That selected an earlier reference arm, not the final live arm, so its
wall times are NOT exact live reproductions. r90 directly calls the same
production reference evaluator in the diagnostic translation unit and asserts
that direct applied reason/time match the live result before inspecting its
tube. Preserve r89 as a rejected diagnostic method, not passing evidence.

Producer: centered_step already calculates both the natural interval image
and a mean-value enclosure of the same shared midpoint body/tire map. It uses
the latter alone except for tire refinement and discontinuous branches. A
mean-value bound can be wider than the independent natural image; discarding
the available second bound carries unnecessary width into later samples.
Intersect the two outward-rounded enclosures before adding back Cartesian
translation. The intersection contains every represented response. Empty
intersection is a numerical failure, never an accepted or repaired interval.
Keep all input members, hybrid modes, equations, steps and full-rest checks.

The isolated r91variant (no production change) accepts the original964request
through full rest12.494999750and preserves963acceptance. All sampled scalar
responses remain enclosed. This motivates, but does not replace, numerical
verification over continuous inputs and nonsmooth mode boundaries.

Same-world architecture audit: persistentA nominal succeeds (30.1416ms),
complete-restY succeeds (15.492ms,u=0). Side-specific B/C/D/G candidates are
rejected before solving in this no-target scene; their feasibility is
inconclusive. No applied/live authority or general infeasibility follows.

Regression: preserve original964source/history and final prepared programme;
retain an unchanged wall-map region with the original global grid geometry,
treat all omitted cells as Unknown. Assert the unchanged full swept footprint
stays clear until complete rest; show failure on baseline first. Run broad
continuous/hybrid/native enclosure tests, positive/negative certificate and
source replays, package/build, then fresh dev2. Retire only the choice to
discard the already computed independent bound. No new authority or parameter.
Publication-clock agreement and live25ms remain separate unaccepted criteria.

## Verification

Baseline regression fails at the original12.25499975wall time. Shared
production intersection passes it, buildr44=26packages/22.4s andtestsr37=
2433records/62groups/zeroerrors-failures-skips. Enclosure-r14passes2017600
continuous and956800hybrid scalar values plus independent rounding/footprint/
point/duration boundary checks. Four original-source suites pass.

Native r93D1964/source260accepts full applied certificate, production adapter,
Stop materialization and join, rest12.494999750, minimum peer1.41015037m,
42programme commands/271checked samples,19.54ms isolated local replay.
R19D1972wall failure also accepts; r19D21071peer rejection remains unchanged.
Positive r17and signed r16joins and negative r18real history gap remain correct.
R19slow state rejects retain their original time and roughly14/12mscost.
None of these local timings establish integrated25ms. See
[evidence](wall-enclosure-evidence.json). Freshdev2-r21and remaining M4–M6next.

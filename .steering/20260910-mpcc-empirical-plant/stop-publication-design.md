# Execute the terminal Stop that was actually certified

Baseline/rollback `2b0293d1`. Dev2-r3 first moving Emergency: D2decision957,
now10.939999755, world18325234050950069086. Simulator torn down; preserve run.

At954 the independent Stop414 is accepted as a latest-state feedback Bundle.
Its original cursor steering is0.161824rad; the reachable certified command is
0.331551rad. Its original5s solved suffix does NOT certify in the new world.
The accepted proof instead contains a0.6s terminal contingency: the first25ms
uses wire acceleration0.220227, then wire−3 through native body rest.
The live boundary labels this Stop and marks unmodified414 executed. That is
the earliest broken invariant: it discards the actual certified terminal input
sequence and claims execution of different source controls. At955 it can repeat
the leading nonbraking command;956publishes normal380;957loses all authority.
Do not attribute the entire957failure to the ledger alone: peer/ego evolution
and normal reselection also contribute, and integrated repair is still required.

The954alternate Request itself was not captured. Diagnostic reconstruction uses
the exact954current state/peer Request with immutable414 and the live code's
TimeAlignedCandidate clock. This is labelled reconstruction, not exact captured
alternate execution. The exact957Request/source414 replay also rejects.
Evidence: `output/20260910-nine-state-stop-publication-r1` and
`output/20260910-nine-state-stop-materialization-r1`, corresponding revalidation
folders. Zero solves; unchanged geometry, margins, tolerances and inputs.

The independent comparison seals the already proved terminal samples through
the existing Stop bundle builder. It produces a fresh0.6s artifact, initial
steering0.331551, first acceleration0.220227 and second−3. Native replay and the
current-world join both pass, with complete solved rest suffix and no feedback
rebase. No new fallback, wait/hold rule, target, optimization or acceptance
relaxation is introduced.

Production: retain the accepted revalidation Result by move. At the final Stop
selection boundary, if the solved suffix was not the terminal certificate,
seal its actual terminal trajectory/input samples under a new identity and
revalidate before publication. Never promote a stateless source as unmodified
execution, including a Stop-labelled source. Preserve the Stop role when its
published Bundle is revalidated; a certified new normal may still replace it.
Keep actual initial bounds from the terminal producer, not guessed first-sample
bounds. Materialization must preserve the first serialized command and must
yield a complete-rest suffix; otherwise reject closed.

Tests must exercise feedback→materialized Stop→next braking command→native rest,
identity/clock mismatch rejection and unchanged first command. Then build,
package tests, sealed954comparison and a fresh dev2. M4–M6 remain open.

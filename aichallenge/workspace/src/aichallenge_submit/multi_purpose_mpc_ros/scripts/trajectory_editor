#!/usr/bin/env python3

import sys
from pathlib import Path

source_pkg_root = Path(__file__).resolve().parents[1]
if str(source_pkg_root) not in sys.path:
    sys.path.insert(0, str(source_pkg_root))

from multi_purpose_mpc_ros.tools.trajectory_editor import main


if __name__ == "__main__":
    main(sys.argv[1:])
